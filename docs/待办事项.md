# 发运单生成器 - 待办事项

## 2026-06-06 待办

### 🔴 紧急

- [ ] 修复单击下拉框不弹出的问题
  - 尝试方案1：使用QMenu作为选择菜单
  - 尝试方案2：使用自定义弹出窗口
  - 尝试方案3：修改编辑触发机制

### 🟡 重要

- [ ] 优化下拉框的显示样式
- [ ] 添加快捷键支持（Ctrl+S保存等）
- [ ] 改进错误提示信息

### 🟢 一般

- [ ] 添加撤销/重做功能
- [ ] 添加打印功能
- [ ] 添加帮助文档

### ⚪ 未来

- [ ] 打包成EXE文件
- [ ] 添加应用图标
- [ ] 创建安装程序
- [ ] 支持多语言

---

## 已完成

- [x] 项目初始化
- [x] 数据模型定义
- [x] 树结构构建器
- [x] 计算引擎
- [x] 导出器
- [x] 主窗口界面
- [x] 配置对话框
- [x] BOM数据解析器
- [x] 复选框功能
- [x] 批量设置功能
- [x] 保存/加载进度
- [x] 检验功能
- [x] 预览功能
- [x] 导出功能

### 2026-08-07（v2.5 界面交互修复）

- [x] Delete 快捷键不再劫持文本输入（收紧为树控件 WidgetShortcut）
- [x] 勾选操作可撤销、可重做并标记未保存（含 F2/F3、Shift/Ctrl 路径、祖先联动）
- [x] refresh_tree 信号重连 try/finally 保护（异常自愈）
- [x] 新建/删除预设不再覆盖表格未保存编辑（blockSignals 保护）
- [x] 导入/计算/导出显示忙碌状态并防重复点击（_busy 上下文管理器）
- [x] 清理裸 except 静默吞错与 BatchConfigDialog 死代码
- [x] 新增 test_v25_ui_fixes.py 回归测试（34 项，全量回归通过）

### 2026-08-08（v2.5.1 发运主体预设修复）

- [x] 新建预设后立即设为当前预设，随后"保存"把内容写入新预设（修复 v2.5 blockSignals 回归）
- [x] 重置预设后下拉框与文件同步刷新，切换不再报"预设不存在"
- [x] 预设/定义写入改为原子替换（tmp + os.replace），写入中断不再损坏配置
- [x] 保存/新建预设名称为空时明确提示，不再静默返回误导用户
- [x] 新增 test_v251_preset_fixes.py 回归测试（22 项；全量回归 6 脚本 188 项 + pre_commit_verify 4496 项通过）

### 2026-08-08（v2.5.2 发运主体对话框初始化同步修复）

- [x] 打开"发运主体定义"对话框时，明细表格初始内容与下拉框（当前预设）一致，不再出现"下拉转载机、明细输送机"
- [x] 当前预设不存在时回退到全局发运主体定义
- [x] 新增 test_v252_preset_dialog_init.py 回归测试（10 项；全量回归 7 脚本 198 项 + pre_commit_verify 通过）

### 2026-08-08（v2.5.3 批量设置同级同父校验）

- [x] 批量设置仅允许修改同一层级且同父物料的勾选节点，跨层级/跨父选择直接阻止并提示，防止把父节点"是否展开"改为"否"导致其下内容被折叠隐藏
- [x] 新增 test_v253_batch_sibling_guard.py 回归测试（13 项；全量回归 8 脚本 211 项 + pre_commit_verify 通过）

---

## 下次开发重点

1. **首先解决下拉框问题** - 这是用户最需要的功能
2. **测试所有功能** - 确保没有回归问题
3. **优化用户体验** - 根据用户反馈调整
