# 更新日志

## v2.3.2 (2026-08-05)

本版本为代码全面审查（2026-08-05）驱动的数据正确性与语义一致性修复，涉及拆分物料、
删除顶级节点、展开状态各入口语义统一、撤销恢复数量重算。

### 修复（拆分物料）
- **第二部分数量算错**：拆分非顶级叶子节点时，第二部分节点在挂载到父节点前按顶级节点
  计算最终数量（漏乘父节点倍数），导致拆分件发运数量偏小。现在先建立父引用、挂载到父
  节点后再重算最终数量。
- **父引用缺失**：第二部分节点未设置 `parent`，导致隐藏判断（祖先"是否展开=否"边界）
  失效，被拆分的第二件在"否"边界下仍可见、可操作。现在显式设置 `parent`。
- **备注丢失**：第二部分节点未继承原节点备注，现在一并复制。
- **顶级拆分静默丢失**：拆分顶级叶子节点时第二部分无处挂载（单根模型），原实现静默丢弃
  且不参与计算。现在明确提示"顶级节点不能拆分"。

### 修复（删除顶级节点）
- **删除后仍显示并参与计算**：删除顶级节点后 `tree_builder.root` 仍指向已删除节点，
  树控件继续显示整个子树、计算仍包含被删数据。现在删除顶级节点后清空 root 引用，
  单个删除（delete_material）与批量删除（delete_checked_nodes）行为一致。

### 修复（展开状态语义一致性）
- **设"否"两种入口行为不一致**：右键菜单/Y-N 快捷键设"否"不清除子孙配置，改回"是"时
  隐藏期间保留的配置会意外复活为发运单元。现在 `set_node_expand` 与"是否展开"列点击
  路径一致：子孙已维护配置时先确认，确认后清除子孙配置。
- **批量设置"展开=是"残留配置**：批量设置选择"是"时不清理主体/方式，界面残留旧值。
  现在与单节点路径一致：设为"是"即清空发运主体/方式/备注，备注输入框同步禁用。
- **拦截条件缺备注**：右键/Y-N 设"是"的拦截只查主体/方式、不含备注，与列点击和
  "清空发运配置"入口判定不一致。现在三者统一：主体/方式/备注任一存在即拦截。

### 修复（撤销恢复数量）
- **恢复后数量可能残留旧值**：快照不含 final_quantity，`_restore_state` 依赖 add_child
  顺带重算；顶级节点数量曾被修改或输入行序非拓扑时，恢复后子孙数量可能算错。
  现在恢复后自顶向下统一重算整棵树（与建树逻辑一致），撤销/恢复后数量与快照严格一致。

### 新增测试
- `test_v232_fixes.py`（45 项）：拆分挂载/数量/父引用/备注、顶级拆分禁止、单个与批量
  删除顶级节点后的 root 清空、展开状态三入口语义统一、批量设"是"清配置、恢复后整树重算
  （含非拓扑行序场景）。

## v2.3.1 (2026-08-05)

本版本为生产事故驱动的修复：粘贴格式导致整棵树塌缩、已维护节点无法改回"是否展开=是"、塌缩后撤销够不着、以及打包 EXE 在不可写目录下"发运主体配置保存后丢失"。

### 修复（粘贴格式与树塌缩）
- **粘贴塌缩**：复制"否+主体+方式"格式粘贴到勾选的顶级/高层节点时，整棵子树被隐藏、界面只剩最高级。现在粘贴前逐节点过滤：顶级节点、隐藏节点、粘贴"是"时的叶子节点、粘贴"否"时子孙已维护配置的节点一律跳过，并分类提示跳过数量与原因；全部被跳过时明确告知未做任何修改。
- **隐藏节点参与批量操作**：勾选收集、全选、反选统一跳过隐藏节点，消除"先勾选→祖先变否→残留勾选被静默粘贴/删除"的隐患。
- **展开状态串位**：`refresh_tree` 恢复展开状态的键由物料号改为节点稳定 uid，消除同物料号多实例（拆分场景）刷新后互相串位。

### 修复（是否展开改回"是"）
- 已维护发运配置的节点，右键菜单/快捷键 Y 的拦截提示原本指向不存在的"清空发运配置"入口，形成死路。现在右键菜单新增"🧹 清空发运配置"（带确认、可撤销、不动子孙），先清空即可改回"是"；拦截文案同步指向该入口。单元格点击列6的确认清空路径保持不变。

### 修复（撤销/恢复）
- **深度不足**：撤销深度由 5 步提升至 30 步（实测 5000 节点约 16MB），解决事故后自救操作把事故前快照挤出栈、撤销永远回不去的问题。
- **静默失败**：撤销/恢复改为"先恢复、成功后才移动快照"；恢复异常时弹出错误框提示保存进度，栈保持不变、可重试，不再因一次错误导致整个撤销系统失灵。
- **无反馈**：每次成功撤销/恢复后状态栏显示"已撤销（还可撤销 N 步）"。

### 修复（发运主体配置保存后丢失）
- 程序所在目录不可写时（如 `C:\Program Files`、只读共享盘），发运主体定义/预设写入失败但界面仍提示"已保存"，重开程序后配置丢失。现在：保存失败时弹错误框展示真实原因与配置目录路径、对话框保持打开便于处理后重试；读写失败统一记入 error.log；新建预设失败时区分"已存在"与"写入失败"。

### 新增测试
- `test_v24_paste_expand_fixes.py`（45 项）：粘贴保护、隐藏节点排除、清空配置入口、uid 恢复、塌缩后撤销/恢复全链路。
- `test_v231_fixes.py`（15 项）：配置写入失败的返回值/错误暴露/UI 不谎报成功。

## v2.3.0 (2026-07-22)

本版本为发布前代码审查（详见 `docs/代码审计报告_2026-07-22.md`）驱动的稳定性修复，重点解决两个会损害交付发运单正确性的打包阻塞问题，并重建撤销系统。

### 修复（打包阻塞级）
- **发运主体名双前缀**：修复全新启动后预览/导出的发运单"发运主体"列显示 `01-01-机头`（应为 `01-机头`）的问题。该缺陷界面显示正常、仅交付物错误，隐蔽性强。通过统一发运主体静态缓存的格式契约（一律存不带前缀的纯名称）解决。
- **onefile 配置静默丢失**：修复打包后用户自定义的发运主体/预设"保存成功"但程序退出即丢失的问题。配置与日志改为写入 EXE 所在目录（可写），首次运行自动从包内出厂配置播种，兼顾"读到出厂内容"与"修改可持久化"。

### 修复（撤销系统重建）
- **撤销键冲突**：快照键由 `(父物料号_物料号)` 复合键改为节点稳定 `uid`，消除拆分物料、同物料多实例场景下撤销丢失或错置节点的问题。
- **幽灵节点**：撤销"添加/拆分"后，被移除的节点不再残留在 `all_nodes` 中污染计算、保存与统计。
- **跨数据集撤销污染**：导入新 BOM、加载进度文件后自动清空撤销/恢复栈，避免旧数据快照被撤销复活进新数据集。
- **变更入口补全**：批量设置、添加物料、双击编辑节点等操作现在均可撤销，并正确标记"未保存"。
- **重复快照**：修复 Y/N 快捷设置展开时产生两个相同快照、导致首次撤销无可见效果的问题。

### 修复（数据正确性）
- **总重计算**：总重改为逐行求和（Σ 数量×净重），不再用"求和数量×首个净重"覆盖，同物料在不同父节点下净重不同时也正确。
- **数量 NaN 守卫**：计算结果含空数量（NaN）时明确报错，而非取整后输出巨大垃圾值。
- **行序无关**：建树后自顶向下统一重算最终数量，BOM 行序非拓扑序时不再静默算错。
- **规格保留**：拆分名称时仅在原"规格"为空才填充，不再覆盖 ERP 自带的规格。
- **拆分重载保真**：进度恢复改为按行序逐一对应（而非按物料号+父物料号首匹配），修复拆分物料存盘重开后第二个单元配置丢失的严重数据错误。
- **项目信息 nan**：项目信息表空字段读回为空串，不再显示/导出 "nan"。

### 修复（稳定性）
- **CSV 编码回退**：CSV 导入按 UTF-8 → GBK → GB18030 依次尝试，兼容 ERP 常见的 GBK 导出，不再直接崩溃。
- **空表/缺列校验**：导入空表或缺必需列时给出清晰中文错误，而非英文 KeyError 堆栈。
- **异常弹窗**：全局异常钩子在记录日志的同时弹窗提示，避免打包后崩溃表现为"静默闪退"。
- **代码排序校验**：发运主体定义排序对非数字代码（用户自定义）容错，不再因 `int()` 抛 ValueError 闪退。
- **关窗保存**：关闭时选择"保存"但被取消或失败，不再直接退出，避免未保存丢失。

### 修复（界面交互）
- **复选框命中区**：改用系统样式动态定位复选框指示器，修复 Fusion 下点复选框本身不触发"勾选子级自动取消祖先"的问题。
- **对话框乒乓**：`on_item_changed` 增加重入保护，修复对含配置节点改"是否展开"一路点"否"时确认对话框无限交替弹出。
- **自动规则**：单击选择发运主体也套用 98→B打捆 / 99→C装箱 规则，节点正确构成发运单元。
- **快捷键劫持**：Y/N 快捷键限定为树控件获得焦点时生效，不再拦截文本输入框里的 y/n 字符。

### 修复（撤销与保存提示）
- **结构变更标脏**：右键设置发运主体/方式、拆分、删除、格式刷等所有可撤销操作统一标记"未保存"，关窗能正确提示。
- **批量空跳过**：批量设置若勾选节点全被跳过（无实际变更），不再产生无意义的撤销点。

### 优化与清理
- 代码内默认发运主体定义与出厂配置对齐（补 21/22 推移梁/推移座，90=自定义改动）。
- 移除导出器在当前目录预建无用 `output/` 的构造副作用。
- 进度加载按工作表名 `BOM数据` 显式读取（旧文件回退第一个表），避免调换 sheet 顺序读错。
- 物料号去除数值型读入产生的 `.0` 后缀（12345.0 → 12345）。
- `helpers.get_entity_name` 改为返回纯名称，消除与 `bom_node.get_entity_name` 的同名异义陷阱。
- 补上 `QPlainTextEdit` 导入，拆除 `PlainTextEditDelegate` 的潜在 NameError。

### 新增
- 打包感知的应用目录解析（`utils/helpers.py`）：区分只读资源目录（`get_bundle_dir`）与可写数据目录（`get_writable_app_dir`），并提供出厂配置播种（`seed_file_from_bundle`）。
- v2.3 针对性回归测试（`test_v23_fixes.py`，25 项行为级断言），弥补 `pre_commit_verify.py` 仅检查符号存在性的盲区。

### 已知遗留（列入 v2.3.1，发布前审慎保留）
以下项在发布前夕评估为"低收益/高回归风险"或需额外资源，本次未改，留待下一版：
- **性能**：导出 Excel 逐单元格设置样式、建树父节点兜底查找为 O(n²)，万行级 BOM 可能卡顿（当前数据量无影响）。
- **PDF 导出中文乱码**：PDF 功能用 Helvetica 默认字体，中文输出异常；需中文字体资源，若不使用 PDF 可后续移除。
- **部分死代码**：不可达的 `ComboBoxDelegate`、未接线的菜单/方法与无用 import 等，大范围删除有回归风险，未清理。
- **配置缓存**：`EntityConfigManager` 类级缓存进程内不失效，外部直接改 JSON 后运行中程序不感知。
- **多根选取**：撤销快照根节点取"最后一个 level-0"、建树取"第一个"，仅在多根异常数据下有差异（正常单根无影响）。

---

## v2.2.0 (2026-06-23)

### 重大改进
- **代码架构重构**：将 main_window.py 从 3881 行拆分为 10 个模块，每个模块 178-700 行
  - `ui/tree_mixin.py` — 树结构操作
  - `ui/config_mixin.py` — 节点配置操作
  - `ui/batch_mixin.py` — 批量操作和物料管理
  - `ui/undo_mixin.py` — 撤销/恢复
  - `ui/progress_mixin.py` — 进度保存/加载
  - `ui/preview_mixin.py` — 计算、预览、导出
  - `ui/entity_mixin.py` — 发运主体定义 UI
  - `core/entity_config.py` — 发运主体配置管理（独立数据层）
  - `core/progress_config.py` — 进度文件读写（独立数据层）

### 修复
- **发运主体映射错误**：helpers.py 中的 get_entity_name() 映射与 bom_node.py 不一致，改为委托给统一数据源
- **发运主体配置保存**：修复修改发运主体配置后重新打开不生效的问题，保存时同时更新全局定义和当前预设
- **BOMNode 计算副作用**：__post_init__ 中仅在 parent=None 时自动计算，避免创建子节点时的错误计算
- **撤销/恢复冲突**：修复同物料多实例场景下 material_id 冲突，改用复合 key (parent_id_material_id)
- **bom_parser 步骤编号**：修正 _clean_data() 中重复的步骤编号

### 优化
- **统一数据源**：发运主体映射统一由 EntityConfigManager 管理，消除多处重复定义
- **删除死代码**：移除未使用的 ConfigManager 和 Logger 模块
- **配置管理独立**：发运主体配置和进度文件读写从 UI 层分离到独立的数据层

---

## v2.0.0 (2026-06-18)

### 新功能
- **窗口标题状态显示**：导入BOM后标题显示文件名，修改后标记"*未保存"，保存后恢复正常
- **备注列双击编辑**：双击备注列直接在格内编辑，支持单行快速输入
- **批量设置备注**：右键菜单支持"设置备注到勾选节点"，一次性为多个节点设置相同备注
- **添加子物料**：右键节点可直接添加子物料，自动继承父节点关系
- **父子联动清除**：父物料设为"否"时，自动检测并清除子物料发运配置，防止幽灵数据进入统计

### 优化
- **导入覆盖提示**：优化导入新BOM时的覆盖提示逻辑，修复`apply_all_configs`缺失bug
- **展开/折叠联动**：设为"是"时自动展开下一级，设为"否"时自动折叠
- **加载进度恢复**：加载进度后自动恢复树的展开状态
- **事件过滤优化**：双击事件精确控制，列6/7/8只响应单击菜单，列9双击编辑，其他列双击展开/折叠
- **撤销支持**：备注编辑支持Ctrl+Z撤销

### 修复
- **修复导入崩溃**：`_do_import_bom`缺少pandas导入和`apply_all_configs`方法调用
- **修复重复撤销**：备注编辑时重复调用`_save_state()`导致撤销失效
- **修复双击冲突**：`_set_item_value`中`setText`触发信号导致逻辑重复执行
- **清理死代码**：移除`_is_node_hidden`中的重复return语句

---

## v1.1.0 (2026-06-10)

### 新功能
- **物料拆分**：右键菜单支持将一个物料拆分为两个发运单元，手动输入数量、发运主体和方式
- **撤销/恢复**：支持 Ctrl+Z 撤销、Ctrl+Y 恢复，最多5步，覆盖修改配置和删除操作
- **预设自动加载**：发运主体定义窗口切换预设时自动加载，无需手动点击
- **项目总重**：预览面板和导出Excel中显示项目总重
- **98/99自动设置**：选择98-捆装发运类自动设B打捆，99-整合装箱类自动设C装箱
- **序号排序**：树结构按BOM序号排列子节点，反映装配顺序

### 优化
- **BOM字段拆分**：支持单空格分隔、项目代码跳过（LS/LZ/LC/ZT等）
- **材质过滤**：增强Q355B等带后缀字母的材质过滤，支持27SiMn(禁用)格式
- **厚度过滤**：δ40、δ12等板材厚度信息自动过滤
- **工艺路线过滤**：单字母工艺路线（A、Z）自动过滤
- **边界节点锁定**：叶子节点不能设为"展开"，有发运配置的节点不能改回"是"
- **列边框**：树视图和表格添加清晰的列分隔线
- **发运主体动态加载**：下拉菜单从配置文件动态加载，支持新增的发运主体

### 修复
- **树结构父子关系**：修复同一物料在不同父节点下子节点挂载错误的问题
- **05S01-53展开**：修复ls100中共享组件无法展开的问题
- **加载进度nan**：修复加载保存的进度时空格子显示"nan"的问题
- **导出模板**：修复导出/导入发运主体模板缺少pandas导入的问题
- **发运主体定义**：修复选择预设不自动加载的问题
- **编辑权限**：修复所有节点无法编辑发运主体/方式的问题

---

## v1.0.0 (2026-06-08)

### 初始版本
- BOM数据导入与解析
- 多级树结构可视化
- 发运配置维护（是否展开、发运主体、发运方式、备注）
- 发运单计算与预览
- Excel/CSV导出
- 配置持久化（保存/加载进度）
- 发运主体定义与预设管理
